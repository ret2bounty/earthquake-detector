:name: Earthquake Detector

# 1. Load the platform (now includes the smart sensor logic)
mach create
machine LoadPlatformDescription @earthquake.repl

# 2. Load the software (`software.elf` compiled using `main.c` and `start.S`)
sysbus LoadELF @software.elf

# 3. Open UART to see the alerts
showAnalyzer uart

# 4. Start!
start